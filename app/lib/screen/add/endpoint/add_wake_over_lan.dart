import 'package:flutter/material.dart';

class AddWakeOverLan extends StatelessWidget {
  const AddWakeOverLan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Wake over LAN");
  }

}